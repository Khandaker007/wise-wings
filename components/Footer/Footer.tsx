import style from './footer.module.scss';
const { 
    footer
} = style;

const Footer = () => {
    return (
        <footer className={footer}>
            <h1>Footer</h1>
        </footer>
    );
};

export default Footer;
